# CLAUDE.local.md

Personal notes for this repo (gitignored). Project instructions live in `AGENTS.md`; `.claude/settings.json` sets `instructionFiles` so this file loads alongside it instead of replacing it.

## Context

Solo hobby project. I know the codebase — no need to over-explain basics or hedge.
