# Committed on purpose (see .gitignore) so CI builds get the license key.
# VITE_* values are inlined into the public bundle — never put a secret here.
VITE_PRIMEUI_LICENSE_KEY=ey_SOMETHING_SECRET_SOMETHING_SENSITIVE